# Copy to supabase.paste.env and paste your values from Supabase.
#   cp supabase.paste.env.example supabase.paste.env
#
# Database → Connect → Prisma:
#   • DATABASE_URL  = "Transaction" pooler (port 6543, ?pgbouncer=true)
#   • DIRECT_URL    = "Session" or direct (port 5432)
#
# Paste the FULL connection strings (including quotes optional):

DATABASE_URL=
DIRECT_URL=
